# PRD: AI 기반 회계감사 분석 플랫폼 (가칭)

- 작성일: 2026-07-20
- 최종 수정: 2026-09-02
- 작성자: (사용자명 입력) / Claude Code
- 문서 상태: Draft v3.2 — **구현 현황 반영**. 초안(v2.0) 대비 실제 구현이 달라진
  부분과 그 사유를 각 FR 하단 `구현 메모`에 명시했고, 초안에 없던 기능
  (FR-8 시산표 검증, FR-9 전표 부정위험 테스트, FR-10 중요성 산정,
  FR-11 미수정왜곡사항 집계표, FR-12 기준서 AI 챗봇)을 추가했다.
- 대상: 7일 웹서비스 개발 대회 출품작 + 회계법인 취업 포트폴리오
- 배포: https://web-phi-two-16.vercel.app (GitHub push 시 Vercel 자동 배포)

---

## 1. 개요 (Overview)

표본감사가 아닌 **전수 데이터 분석**을 통해 재무제표 이상징후와 부정거래 가능성을
자동으로 탐지하고, 감사인의 시각에서 재무제표를 분석해 위험요인과 추천 감사절차를
제시하는 웹 기반 감사보조 분석 도구. 이상징후와 부정거래 가능성을 조기에 탐지함으로써
**회계투명성 확보**에 기여하는 것을 핵심 가치로 삼는다.

공인회계사 자격을 취득하고 회계법인 입사를 앞둔 개발자가, 실무에서 실제로 쓸 수 있는
수준의 분석 로직을 직접 웹서비스로 구현하여 대회에 출품하고 취업 포트폴리오로도
활용하는 것을 목표로 한다.

### 1.1 포지셔닝 (2026-07-24 갱신)

개발 도중 "대형 회계법인 파트너 관점에서 이 도구가 실제 감사에 도움이 되는가"를
스스로 점검한 결과, 초기 카피에 있던 **"전수 데이터 자동 부정탐지"** 표현은
과장이라고 판단해 문구와 기능 구성을 다음과 같이 재정렬했다.

- 이 도구의 산출물은 **감사증거도 감사결론도 아니며**, ISA에 근거해 "더 들여다볼
  곳"을 좁혀주는 **스크리닝 지표**다. 화면·리포트 전반에 이를 고지한다.
- "전수 분석"이라는 표현은 사용자가 **전표 원장을 실제로 업로드한 경우로 한정**해
  사용한다. DART 공시 기반 경로는 요약 재무제표만 보므로 전수가 아니다.
- 감사인이 실제로 만지는 데이터는 클라이언트 ERP에서 추출한 **시산표(TB)·
  총계정원장(GL)·전표(JE)** 이고, 감사는 재무제표가 확정되기 *전에* 증거를 모으는
  작업이다. 이 간극을 메우기 위해 초안에 없던 **FR-8(시산표 검증)** 과
  **FR-9(전표 부정위험 테스트)** 를 추가했다.
- 이에 따라 화면 상단 탭도 성격에 맞게 두 갈래로 묶었다 — **재무제표 스크리닝**
  (공개·요약 데이터로 위험 신호를 1차 선별) / **감사 실무**(클라이언트 원장 기반
  실증 절차·표본·조서).

## 2. 문제 정의 (Problem Statement)

- 전통적 감사는 시간/인력 제약으로 **표본감사**에 의존하며, 표본에 포함되지 않은
  거래의 이상징후는 놓치기 쉽다.
- 재무제표 분석 시 신입/주니어 감사인은 어떤 계정을, 어떤 기준으로, 어떻게
  이상 여부를 판단해야 하는지에 대한 체계적인 가이드가 부족하다.
- 재고실사, 표본추출 등에서 통계적으로 타당한 샘플 크기 산정이 수작업/경험치에
  의존하는 경우가 많다.
- 분식회계·부정거래는 개별 계정이 아니라 **여러 지표의 조합**(매출 급증 + 매출채권
  급증 + 현금 감소 등)에서 신호가 나타나는데, 이를 수작업으로 교차 확인하기 어렵다.

## 3. 목표 (Goals)

1. 재무제표(DART 공시자료 또는 비상장법인 엑셀)를 업로드/조회하면 **자동으로
   재무비율, 이상 변동 계정, 부정징후 점수**를 산출한다.
2. 학술적으로 검증된 모델(Benford's Law, Beneish M-Score, Altman Z-Score) 및
   실무 감사기법(RSF, 라운드트립 거래 탐지)을 적용해 분석 결과에 **객관적
   근거**를 부여한다.
3. 탐지된 위험 신호에 대해 **추천 감사절차(체크리스트)**를 자동 매칭해 실무
   적용 가능성을 보여준다.
4. 표본감사 설계에 필요한 **통계적 샘플 크기(MUS)**를 자동 계산해준다.
5. 결과를 대시보드 및 **분석적검토 조서(초안) 형태의 리포트(PDF/Word)**로
   export한다.
6. Upstage AI(Solar LLM)를 활용해 **주요 공시 자동 요약**을 제공한다.
7. 코딩 비전공자도 7일 내 완성 가능한 기술적 난이도로, 확정된 스택
   (Next.js/Supabase/Vercel/GitHub) 안에서 구현한다.

### Non-Goals (범위에서 제외)

상세 제외 항목과 사유는 **14. 범위 제외 (Out of Scope)** 섹션 참고.

## 4. 타겟 사용자 (Target Users)

- **주 사용자**: 회계법인 소속 감사인(주니어~시니어), 공인회계사 수험생/합격자
- **부 사용자**: 대회 심사위원, 채용 담당자 (포트폴리오 시연 대상)
- 사용자는 회계/감사 도메인 지식은 충분하나, 데이터 분석 도구 사용 경험은
  많지 않을 수 있음 → UI는 전문 용어를 그대로 쓰되 조작은 단순해야 함.

## 5. 사용자 시나리오 (User Scenarios)

### 시나리오 1. 상장기업 정기감사 — DART 연동 경로

- **사용자**: 회계법인 주니어 감사인 A
- **상황**: 상장기업 B사의 연간 재무제표 감사를 앞두고, 위험 계정을 사전에 파악하고 싶다.
- **흐름**:
  1. 앱에 접속해 "DART 공시자료 조회"를 선택하고 B사를 검색한다. (FR-1.1)
  2. 사업연도와 개별/연결 재무제표 여부를 선택해 최근 재무제표를 불러온다.
  3. 자동 계산된 재무비율과 전기 대비 이상 변동 계정을 확인한다. (FR-2)
  4. Benford's Law, Beneish M-Score, Altman Z-Score, RSF 테스트, 라운드트립
     거래 탐지 결과를 확인해 분식 위험이 높은 영역을 파악한다. (FR-3)
  5. 탐지된 위험 신호에 대해 자동 생성된 감사 체크리스트를 확인한다. (FR-4)
  6. B사의 최근 주요 공시에 대한 AI 요약과 확인 포인트를 확인한다. (FR-7)
  7. 분석 결과를 감사조서 형태 리포트로 export해 팀 회의 자료로 활용한다. (FR-6)
- **기대 결과**: 감사 착수 전 위험 계정과 절차를 사전에 파악해, 표본감사 설계 시간을
  단축하고 놓치기 쉬운 계정을 미리 짚어낸다.

### 시나리오 2. 비상장 중소기업 감사 — 엑셀 업로드 경로

- **사용자**: 회계법인 감사인 C
- **상황**: DART 공시자료가 없는 비상장 중소기업 D사를 감사 중이며, 회사가 제공한
  엑셀 파일 기반으로 전수 전표 분석을 하고 싶다.
- **흐름**:
  1. "엑셀 직접 업로드"를 선택하고 표준 템플릿을 다운로드한다. (FR-1.2)
  2. D사의 재무상태표/손익계산서/현금흐름표와 전표데이터를 템플릿 양식에 맞춰
     입력 후 업로드한다. (FR-1.3)
  3. 전표 전수 분석 결과에서 반올림 숫자 거래, cut-off 거래, 작성자-승인자 동일
     건, RSF 이상치, 라운드트립 거래 등 이상 신호를 확인한다. (FR-3.4~3.6, FR-9)
  4. 재무비율과 이상 변동 계정 결과를 함께 검토한다. (FR-2)
  5. 감사 체크리스트를 참고해 추가로 수행할 절차를 결정한다. (FR-4)
- **기대 결과**: 표본감사만으로는 놓칠 수 있는 전수 데이터 상의 이상 패턴을
  포착하고, 비상장기업이라 공시 데이터가 없어도 동일한 분석 프레임을 적용한다.

### 시나리오 3. 재고실사 표본 크기 산정

- **사용자**: 회계법인 감사인 E (재고실사 참여)
- **상황**: 특정 창고의 재고실사 표본 크기를 통계적으로 타당한 근거를 가지고
  산정해야 한다.
- **흐름**:
  1. MUS 샘플링 계산기에 신뢰수준, 허용왜곡금액, 예상오류율, 모집단 총액/건수를
     입력한다. (FR-5.1)
  2. 자동 산출된 표본크기와 계통추출 기준 표본항목 리스트를 확인한다. (FR-5.2)
  3. 산출 근거(산식)를 감사조서에 첨부해 표본설계의 타당성을 문서화한다.
- **기대 결과**: 경험적 추정이 아닌 통계적 근거를 가진 표본크기를 빠르게 확보한다.

### 시나리오 4. 현장 감사 — 클라이언트 원장 기반 실증절차 *(v3.0 추가)*

- **사용자**: 회계법인 인차지 감사인 F
- **상황**: 클라이언트 ERP(더존)에서 시산표와 당기 전표를 추출해 받았고, 재무제표가
  확정되기 전에 부정위험 관점에서 훑어봐야 한다.
- **흐름**:
  1. "감사 실무 > 시산표 검증"에서 시산표를 업로드해 차대변 균형·당기 발생액 일치·
     계정별 roll-forward를 먼저 확인한다. 불일치 계정이 있으면 그 자체가 첫 번째
     질의 대상이다. (FR-8)
  2. "전표(JE) 테스트"에 전표를 올려 ISA 240 기준 8종 예외항목(주말·심야 전기,
     라운드넘버, 적요 공란, 작성=승인, 결산일 임박, 희소 계정, 승인한도 회피)과
     작성자 집중도를 확인한다. (FR-9)
  3. 예외항목이 몰린 계정을 "MUS 샘플링"의 모집단 계정으로 선택해 표본을 설계한다.
     (FR-5.3)
  4. 결과를 "대시보드 & 리포트"에서 분석적검토 조서(초안)로 export해 조서번호·
     중요성 기준·서명란이 들어간 초안 위에 감사인이 결론을 직접 기재한다. (FR-6)
- **기대 결과**: 재무제표 확정 전 단계에서, 공시 데이터로는 볼 수 없는 전표 수준의
  예외항목을 좁혀 실증절차 대상을 정한다.

## 6. 범위 (Scope)

| # | 기능 | 설명 | 구현 |
|---|------|------|------|
| 1 | 데이터 입력 | DART Open API 연동(상장사) + 엑셀 템플릿 업로드(비상장사) + Upstage 기반 재무제표 PDF/이미지 자동인식(비상장사 보완 경로) | ✅ (Supabase 미사용) |
| 2 | 재무비율 자동계산 + 이상 변동 계정 탐지 | 유동성/수익성/성장성/안정성 비율, 전기 대비 증감률 임계치 하이라이트, 연관계정 교차검증 | ✅ (산업평균 비교 ❌) |
| 3 | 이상탐지 모델 | Benford's Law, Beneish M-Score, Altman Z-Score, RSF 테스트, 라운드트립 거래 탐지 (전표 기반 기법은 전표 1만 건 이하 규모로 처리, 14번 참고) | ✅ (FR-3.6 재설계) |
| 4 | 감사 체크리스트 자동생성 | 탐지된 위험 신호 -> 추천 감사절차 + ISA 근거 매칭 | ✅ (LLM 생성 방식) |
| 5 | MUS 샘플링 계산기 | 신뢰수준/허용왜곡/예상오류율/모집단 입력 -> 표본크기/표본항목 산출 | ✅ (계정별 적용) |
| 6 | 대시보드 + 리포트 export | recharts 시각화, 분석적검토 조서(초안) PDF/Word export | ✅ (브라우저 직접 생성) |
| 7 | AI 공시요약 | Upstage Solar LLM 기반 DART 주요공시 요약 + 확인포인트 | ✅ |
| 8 | **시산표(TB) 검증** | 차대변 균형·당기 발생액 일치·계정별 roll-forward 검증 | ✅ (초안 외 추가) |
| 9 | **전표(JE) 부정위험 테스트** | ISA 240 기반 8종 예외항목 추출 + 작성자 집중도 | ✅ (초안 외 추가) |
| 10 | **중요성 산정 워크시트** | ISA 320 — 벤치마크·적용률 선택 → 전반중요성/수행중요성/명백히 사소한 기준 | ✅ (초안 외 추가) |
| 11 | **미수정왜곡사항 집계표(SUM)** | ISA 450 — 왜곡 유형별 집계, 순액·총액을 전반중요성과 비교 | ✅ (초안 외 추가) |
| 12 | **기준서 AI 챗봇** | 커밋된 ISA 요지 코퍼스에 대한 RAG. 근거가 약하면 기권(환각 차단) | ✅ (초안 외 추가) |

> **초안(v2.0) 대비 가장 큰 구조적 변화**: **Supabase는 필수 경로에서 빠졌고,
> 지금은 선택(optional) 백엔드로만 남아 있다.**
> 초안은 corpCode 캐시(FR-1.2)·체크리스트 매핑 테이블(FR-4.1)·업로드 파일과 리포트
> 저장(FR-1.3/FR-6.2)에 Supabase를 쓰기로 했으나, 구현 과정에서 각각 더 단순하거나
> (빌드 시점 JSON 캐시, LLM 생성) 기밀성 측면에서 더 나은(브라우저 내 처리, 서버
> 미전송) 대안을 택했다. **초안이 Supabase에 맡기려던 역할은 하나도 남지 않았다.**
>
> 그 뒤 별도 목적으로 **선택적 서버 저장 계층**을 추가했다(FR-13). `SUPABASE_URL`·
> `SUPABASE_SERVICE_ROLE_KEY`가 설정돼 있을 때만 활성화되고, 없으면 코드가 자동으로
> 브라우저 `localStorage`(`audit-ai-demo-requests`) 폴백으로 동작한다. 즉
> **환경변수 없이도 전 기능이 동작하는 것이 기본값**이다. 어느 모드에서든 거래 단위
> 원장(전표·시산표)은 서버로 보내지도, localStorage에 쓰지도 않고 메모리에만
> 유지한다(기밀성).

## 7. 기능 요구사항 (Functional Requirements)

### FR-1. 데이터 입력
- FR-1.1 Next.js Route Handler(`app/api/dart/*`)에서 **서버사이드로 DART Open
  API를 호출**한다. DART 인증키는 Vercel 환경변수(Environment Variables)로
  보관하고 클라이언트에 노출하지 않는다.
- FR-1.2 DART 고유번호(corpCode) 전체 목록은 매 요청마다 ZIP을 내려받지 않고,
  **최초 1회 배치 작업으로 Supabase Postgres 테이블에 적재(캐싱)** 한 뒤
  `ILIKE` 검색으로 기업명을 조회한다. (Vercel Hobby 서버리스 함수의 10초 실행
  제한 대응 — 8번 비기능요구사항 참고)
- FR-1.3 비상장법인용 표준 엑셀 템플릿(계정과목/전기/당기 고정 컬럼)을 `xlsx`
  (SheetJS) 라이브러리로 파싱한다. 템플릿 파일과 업로드 파일은 **Supabase
  Storage**(private 버킷)에 저장한다.
- FR-1.4 전표데이터(전표번호/일자/시각/계정과목/차변/대변/작성자/승인자/적요)
  업로드를 지원한다 (이상거래 탐지용, `xlsx`로 파싱).
- FR-1.5 두 입력 경로 모두 동일한 표준 데이터 구조(TypeScript 타입/인터페이스로
  정의)로 정규화되어 후속 분석 로직이 입력 출처와 무관하게 동작해야 한다.
- FR-1.6 비상장법인이 표준 엑셀 템플릿을 직접 채우는 대신, 보유한 재무제표
  **PDF/스캔 이미지를 그대로 업로드**하면 **Upstage Information Extraction
  API**가 핵심 재무 지표(자산총계/부채총계/자본총계/매출액/매출원가/
  판매비와관리비/영업이익/영업외수익/영업외비용/법인세비용/당기순이익,
  당기·전기)를 자동으로 인식해 채워준다. 문서는 서버에 저장하지 않고 인식
  즉시 폐기하며, 인식 결과는 사용자가 검토/수정 후 분석 요청에 추가한다.
  (자유 형식 문서 인식이 필요해 FR-1.3의 고정 템플릿 방식과 별도로 제공하는
  보완 경로 — 표준 템플릿이 어려운 사용자를 위한 대안)
- FR-1.7 FR-1.6에서 인식된 영업이익/당기순이익은 손익계산서 산식(매출액-매출원가
  -판관비=영업이익, 영업이익+영업외수익-영업외비용-법인세비용=당기순이익)으로
  재계산한 값과 자동으로 교차검증한다. 인식값과 계산값이 다르면 화면에 경고
  표시(⚠)를 띄워, AI 인식 오류나 원본 문서의 오기재를 사용자가 바로 알아챌 수
  있게 한다.
- FR-1.8 **(추가)** Upstage 등 제3자 AI로 데이터를 전송하는 기능(FR-1.6 자동인식,
  FR-4 체크리스트, FR-7 공시요약)은 최초 사용 시 **명시적 1회 동의**를 받은 뒤에만
  호출한다(`audit-ai-thirdparty-consent`). 업로드 UI에는 상시 주의 문구를 노출한다.
  감사대상 회사명은 LLM에 전송하지 않는다.

> **구현 메모 (FR-1)**
> - FR-1.1 ✅ `app/api/dart/{search,financials,disclosures}` 에서 서버사이드 호출.
>   키는 `DART_API_KEY` 환경변수(Vercel).
> - FR-1.2 ⚠️ **Supabase 미사용으로 변경.** `web/scripts/build-corp-codes.js`가
>   `prebuild` 단계(빌드 시점)에 DART `CORPCODE.xml`(ZIP)을 내려받아
>   `web/data/corp-codes.json`(약 11MB)으로 만들어 저장소에 커밋한다. 런타임에는
>   이 파일을 읽어 `corp_name` 부분일치 검색(상장사 우선 정렬)만 수행한다.
>   → 프로덕션에서 **첫 검색 시 ZIP 다운로드로 응답이 멈추던 문제**를 이 방식으로
>   해결했다. 파싱 시 `parseTagValue: false` 필수 — 없으면 `corp_code`의 앞자리
>   0(`"00126380"`)이 숫자로 변환되며 조용히 손상된다.
> - FR-1.3 ✅ 단, **Supabase Storage 미사용**. 업로드 파일은 서버로 전송하지 않고
>   `excelParse.ts`가 **브라우저에서만** 파싱한다(기밀성 측면에서 초안보다 우수).
>   템플릿 스키마는 `excelTemplate.ts`, 생성기는 `GET /api/template`.
> - FR-1.4 ✅ 전표 시트(`전표데이터`) 파싱 → FR-3/FR-9에서 사용.
> - FR-1.5 ✅ 3개 경로 모두 동일 구조로 정규화(`financials.ts`).
> - FR-1.6 ✅ `POST /api/upstage/extract`. Upstage IE API가 **중첩 객체를 지원하지
>   않아** 필드명을 `자산총계_당기` / `자산총계_전기` 식 접미사로 평평하게 정의.
> - FR-1.7 ✅ `checkIncomeStatement()` — 허용오차 초과 시 ⚠ + 빨간색 경고.

### FR-2. 재무비율 및 이상 변동 계정
- FR-2.1 유동비율, 당좌비율, 부채비율, 이자보상배율, ROE, ROA, 매출총이익률,
  매출채권/재고자산 회전율 등 핵심 비율을 TypeScript 순수 함수로 계산한다
  (연산량이 적어 별도 통계 라이브러리 불필요).
- FR-2.2 계정과목별 전기 대비 당기 증감률 계산, 임계치(기본 ±20~30%, 조정 가능)
  초과 시 하이라이트.
- FR-2.3 연관계정 교차검증 규칙 적용 (예: 매출 증가율 대비 매출채권 증가율 괴리).
- FR-2.4 산업평균 비교는 **동일 업종코드(KSIC)로 DART에 등록된 상장기업들의
  재무제표를 직접 조회해 평균을 계산**하는 방식으로 구현한다 (유료 통계
  데이터셋 미사용).

> **구현 메모 (FR-2)** — `src/lib/ratios.ts`
> - FR-2.1~2.3 ✅ `calculateRatios()`, `calculateAccountChanges()`,
>   `crossCheckAccounts()`.
> - FR-2.4 ⚠️ **코드는 있으나 비활성(데이터 미생성).** 동일 KSIC 상장사 다수의
>   재무제표를 매번 조회하면 DART 호출량·응답시간이 급증해 7일 범위에서 보류했다.
>   이후 `src/lib/industry.ts` + `GET /api/dart/industry`로 **구현은 해뒀다**
>   (업종 인덱스에서 동종 상장사를 찾아 최대 8곳의 재무비율 평균을 산출, 조합별
>   인메모리 캐시).
>   다만 이 로직이 읽는 **`data/listed-industry.json`이 저장소에 없어서** 항상
>   `available: false`("업종 인덱스가 아직 준비되지 않았습니다")를 반환한다.
>   활성화하려면 DART 키를 넣고 한 번만 아래를 실행해 인덱스를 만들어 커밋하면 된다:
>   ```bash
>   cd web && node scripts/build-listed-industry.js   # DART_API_KEY 필요
>   ```
> - 초안에 없던 `calculateValuationRatios()`(EPS/BPS/PER/PBR)와
>   `GET /api/stock-price`(PER·PBR 계산용 주가 조회)를 추가했다.
>   ※ v3.1까지 이 주가 조회를 "Altman Z-Score용"이라고 적었으나 사실과 다르다 —
>   Altman은 시가를 쓰지 않는 Z'-Score다(FR-3.3 구현 메모 참고).

### FR-3. 이상탐지 모델

> **규모 제약(공통)**: FR-3.1, FR-3.4~FR-3.6은 전표(거래) 단위 데이터를
> 전수로 처리한다. Vercel Hobby 서버리스 함수는 10초 실행 제한이 있어
> 대용량 전표 데이터(수만 건 이상) 처리는 **범위 제외 대상**이다(14번 참고).
> 이 프로젝트에서는 전표 1만 건 이하 규모로 처리 범위를 제한하며, 필요 시
> 클라이언트 사이드(브라우저)에서 처리하는 방식을 병행한다. FR-3.2, FR-3.3은
> 재무제표(연간 요약) 단위 계산이라 이 제약과 무관하다.

- FR-3.1 Benford's Law: 전표/계정금액 첫째자리(및 둘째자리) 분포 검정,
  Z-통계량/카이제곱 검정으로 유의성 판단 (TypeScript 순수 연산).
- FR-3.2 Beneish M-Score: 8개 지수(DSRI, GMI, AQI, SGI, DEPI, SGAI, LVGI, TATA)
  계산 및 종합점수 산출, 임계값(-1.78) 기준 위험군 분류 (TypeScript 순수 연산).
- FR-3.3 Altman Z-Score 계열: 부실 예측 스코어 산출 (TypeScript 순수 연산).
- FR-3.4 전표 테스트: 반올림 숫자 빈도, cut-off 거래, 수동분개 비율,
  작성자-승인자 동일 건, 승인 임계치 회피 패턴 탐지.
- FR-3.5 RSF(Relative Size Factor) 테스트: 거래처/계정별 최대 거래금액과 두
  번째로 큰 거래금액의 비율을 계산, 비율이 임계치(예: 10배) 이상이면 해당
  최대거래를 이상치 후보로 플래그 (실무 감사 소프트웨어 ACL/IDEA에서 쓰이는
  검증된 기법).
- FR-3.6 순환거래 네트워크 분석: 전표의 거래상대방을 노드, 거래를 엣지로 하는
  방향 그래프를 구성하고(`graphology` 등 JS 그래프 라이브러리 활용), 사이클
  탐지 알고리즘으로 폐쇄 루프(closed cycle)를 찾아 A→B→C→A 형태의 순환거래를
  식별한다. 결과는 React 기반 그래프 시각화 라이브러리(React Flow 또는
  Cytoscape.js)로 화면에 표시한다. (규모 제약은 상단 공통 안내 참고)

> **구현 메모 (FR-3)** — `src/lib/anomalyDetection.ts`
> - FR-3.1 ✅ Benford 1st-digit. 표본 30건 미만이면 판정하지 않고(통계적 무의미)
>   `null`을 반환한다. 카이제곱 임계값 15.51(자유도 8, α=0.05).
> - FR-3.2 ✅ 8개 지수 + M-Score, 임계 −1.78.
> - FR-3.3 ⚠️ **Z-Score가 아니라 Z'-Score를 쓴다.** 원래 Altman Z-Score의 X4는
>   자기자본의 **시가총액**을 쓰는데, 이 도구의 주 사용처인 비상장기업에는 시가가
>   없어 계산 자체가 불가능하다. 그래서 자기자본을 **장부가액(자본총계)** 으로
>   대체한 Z'-Score 계수(0.717/0.847/3.107/0.42/0.998, 임계 2.9 / 1.23)를
>   사용한다 — 상장·비상장 어느 입력 경로에서도 동일하게 동작한다.
>   (참고: `GET /api/stock-price`는 Z-Score가 아니라 **FR-2.4의 밸류에이션
>   지표(PER/PBR)** 용으로 쓰인다. v3.1까지 이 둘을 뒤바꿔 적어둔 것을 정정했다.)
> - FR-3.4 ✅ → 대폭 확장되어 **FR-9로 독립**했다.
> - FR-3.5 ✅ RSF. 계정과목별로 최대금액 ÷ 차순위금액, 임계 **3배**.
> - FR-3.6 ⚠️ **설계 변경 + 개명.** `graphology` / React Flow / Cytoscape는
>   사용하지 않았다.
>   - 최초 구현은 전표 *라인*을 차변>대변 부호로 짝지어 상대방을 추론했는데,
>     이러면 **정상적인 매출채권 회수·매입채무 지급, 심지어 한 전표의 두 다리까지**
>     순환거래로 잡혀 실제 원장에서는 오탐이 수천 건 나오는 구조였다.
>   - 계정의 **성격**(수익 vs 매입·자산취득)으로 경제적 방향을 판정하고 정산계정
>     (매출채권·매입채무)을 제외한 뒤 판매↔매입을 1:1 매칭하도록 재작성했다.
>     동일 거래처, 30일 이내, 금액 차이 5% 이내를 조건으로 본다.
>     샘플 데이터에서 25건(대부분 오탐) → 1건(진성)으로 줄었다.
>   - 명칭도 과장된 "순환거래 네트워크 분석"에서
>     **"라운드트립(2자간 상계성 거래) 탐지"** 로 바꿨다. **자사 원장 하나만으로
>     A→B→C→A 형태의 3자 순환을 식별하는 것은 구조적으로 불가능**하며(중간
>     당사자들의 장부가 없음), 실제로 탐지 가능한 것은 2자간 되사기 패턴뿐이다.

### FR-4. 감사 체크리스트 자동생성
- FR-4.1 위험지표 -> 추천 감사절차 매핑 규칙을 **Supabase Postgres 테이블**에
  저장하고, FR-2/FR-3에서 탐지된 위험 신호를 조건으로 조회한다.
- FR-4.2 각 위험 신호에 대해 추천 감사절차를 텍스트로 출력, 근거(어떤 지표가
  임계치를 넘었는지) 함께 표시한다.
- FR-4.3 **(추가)** 각 절차에 표시된 ISA 근거를 클릭하면 해당 감사기준서의
  **한글 제목과 요지**를 모달로 보여준다.

> **구현 메모 (FR-4)** — `app/api/upstage/checklist/route.ts`
> - FR-4.1 ⚠️ **Supabase 매핑 테이블 대신 Upstage Solar LLM 생성 방식**으로
>   구현했다. 위험 신호의 조합이 사전에 열거하기 어려울 만큼 다양해 고정 매핑
>   테이블로는 커버리지가 낮았고, 테이블 설계·적재에 드는 시간 대비 효용도 낮았다.
>   감사대상 회사명은 프롬프트에 포함하지 않는다(FR-1.8).
> - FR-4.3 ✅ `ISA_STANDARDS` 상수(ISA 200~720 약 25개)에 한글 제목·요지를 담고,
>   `formatIsaReferenceKo()`가 인용 문자열에서 기준서 번호를 뽑아 한글로 치환한다.
- FR-4.4 **(추가)** 체크리스트에 표시되는 ISA 인용은 **사전 정의된 화이트리스트에
  있는 기준서로 제한**한다. 목록에 없는 인용은 화면에 노출하지 않는다.

> **구현 메모 (FR-4.4) — ISA 인용 화이트리스트 가드** *(2026-07-27 적용)*
>
> LLM이 **존재하지 않는 기준서를 인용**하는 사례가 실제로 관측됐다
> (예: "ISA 515 확정급여제도", "ISA 541 금융상품: 공시" — 둘 다 실재하지 않는다).
> 회계 전문가가 보는 화면에 가짜 기준서가 뜨면 도구 전체의 신뢰가 무너지므로
> 3단으로 막았다.
>
> 1. **단일 진실 공급원**: `ISA_STANDARDS`를 `page.tsx`에서 떼어내
>    `src/lib/isaStandards.ts`로 옮겨 서버(프롬프트)와 클라이언트(렌더링)가 같은
>    목록을 쓰게 했다. 현재 28개 기준서 등재.
> 2. **프롬프트 제한**: 체크리스트 생성 시 허용 기준서 목록(`ISA_ALLOWED_LIST`)을
>    프롬프트에 넣고, 적절한 기준서가 없으면 빈 문자열을 반환하도록 지시한다.
> 3. **응답 차단(실제 방어선)**: 프롬프트만으로는 보장되지 않으므로,
>    `resolveIsaReference()`가 화이트리스트와 대조해 **API 응답 단계에서** 목록 밖
>    인용을 비우고, **렌더링 단계에서** 한 번 더 검사한다. 통과하지 못한 항목은
>    "근거 기준서 미확인 — 감사인이 직접 확인 필요"로 대체하며 클릭도 되지 않는다.
>    모달 역시 원문을 노출하지 않는다.
>
> 검증: `ISA 515` / `ISA 541` / `ISA 999` / 빈 문자열 / 번호 없는 문자열이 모두
> 차단되고, 실재 기준서(`ISA 520`, `ISA 240`)만 통과하는 것을 확인(7/7).

### FR-5. MUS 샘플링 계산기
- FR-5.1 신뢰수준, 허용왜곡금액, 예상오류율, 모집단 총액/건수 입력.
- FR-5.2 표본크기 산출 및 계통추출 기준 표본항목 리스트 생성 (TypeScript 순수
  통계 연산).
- FR-5.3 **(추가)** MUS는 재무제표 전체가 아니라 **계정 단위로** 설계한다.
  모집단은 사용자가 계정과목을 선택하면 해당 계정의 당기 잔액으로 자동 입력된다.

> **구현 메모 (FR-5)** — `src/lib/musSampling.ts`
> - Poisson 신뢰요소(90%=2.31 / 95%=3.00 / 99%=4.61)와 확장계수(1.5 / 1.6 / 2.0)
>   표를 사용한다.
>   `표본크기 = (신뢰요소 × 모집단) ÷ (허용왜곡 − 예상오류금액 × 확장계수)`,
>   `추출간격 = 모집단 ÷ 표본크기`, 계통추출로 표본항목을 생성한다.
> - FR-5.3 ✅ 초안은 모집단을 단일 숫자 입력으로만 상정했으나, 실무에서 MUS는
>   재고자산·매출채권처럼 **실사·조회 대상 계정별로** 설계한다는 지적을 반영해
>   계정 선택 드롭다운(재무상태표 / 손익계산서 optgroup)으로 바꿨다.
> - `isMusEligibleAccount()`가 **총계·소계 계정, 자본 항목, 손익 소계, 주당지표**를
>   모집단 후보에서 제외한다(실증절차로 표본추출할 대상이 아니므로).

### FR-6. 대시보드 및 리포트
- FR-6.1 재무비율/이상탐지 결과를 **recharts**(React 네이티브 차트 라이브러리)로
  시각화 (추세, 산업평균 비교 등).
- FR-6.2 분석 결과를 감사조서 형태 문서로 export한다: PDF는 `@react-pdf/renderer`,
  Word는 `docx`(npm 패키지) 사용. 생성된 파일은 **Supabase Storage**에 저장 후
  signed URL로 다운로드를 제공한다.

> **구현 메모 (FR-6)** — `src/lib/reportExport.ts`
> - FR-6.1 ✅ recharts. 색상은 임의로 고르지 않고 팔레트 검증 스크립트로 대비·채도를
>   계산해 통과한 조합(`#2a78d6` 파랑 + `#eb6834` 주황)만 사용했다.
> - FR-6.2 ⚠️ **PDF 라이브러리 교체**: `@react-pdf/renderer` → **`jspdf` +
>   `html2canvas`**. `@react-pdf/renderer`는 한글 폰트 임베딩이 번거로워, 이미
>   화면에 잘 렌더링되고 있는 DOM을 그대로 래스터화하는 방식으로 바꿨다(다중
>   페이지 분할 처리 포함).
> - ⚠️ **Supabase Storage 미사용** — 브라우저에서 파일을 생성해 바로 다운로드한다.
>   서버에 조서가 남지 않으므로 기밀성 측면에서 유리하다.
> - ⚠️ **명칭 변경**: "감사조서" → **"분석적검토 조서(초안)"**. 이 산출물은 감사
>   조서가 아니라 그 초안이며, 그렇게 부르는 것이 과장이라고 판단했다.
> - 조서 형식 보강: 조서번호(workpaper ref), 대상기간, 중요성 기준, 작성자/검토자
>   서명란, 결론 섹션(감사인이 직접 채우는 공란 포함), 틱마크 범례.

### FR-7. AI 공시요약 (Upstage 활용)
- FR-7.1 DART 공시검색 API로 특정 기업의 최근 공시 목록을 조회하고, 회계/감사
  관점에서 중요한 유형(유상증자, 주요계약체결, 타법인 주식취득, 전환사채발행,
  감사인 지정 등, `pblntf_ty`/`pblntf_detail_ty` 기준 필터링)만 추출한다.
- FR-7.2 선택한 공시 1건에 대해 **Upstage Solar LLM(Chat Completions API)**을
  호출해 3줄 요약과 "감사 시 확인 포인트" 코멘트를 생성한다.
- FR-7.3 (선택) 공시 원문이 스캔 문서/PDF 형태로 제공되는 경우 **Upstage
  Document Parse**로 텍스트를 추출한 뒤 FR-7.2에 전달한다.
- FR-7.4 Upstage API 키는 Vercel 환경변수로 보관한다. Upstage는 완전 무료가
  아닌 크레딧 기반 요금제이므로(가입 시 제공되는 무료 크레딧 범위 확인 필요),
  공시 1건당 LLM 호출 1회로 제한하고 동일 공시 재요청 시 캐시된 결과를
  재사용한다.
- FR-7.5 **(추가)** 요약된 공시 항목을 클릭하면 DART 원문
  (`dart.fss.or.kr/dsaf001/main.do?rcpNo=...`)을 새 탭으로 연다. AI 요약만 보고
  판단하지 않고 항상 원문을 확인할 수 있어야 하기 때문이다.

> **구현 메모 (FR-7)** — `app/api/upstage/disclosure-summary/route.ts`
> - FR-7.1/7.2 ✅ 공시 목록을 한 번의 LLM 호출로 일괄 검토해 중요 항목만
>   골라내고 코멘트를 붙인다(호출량 절감).
> - FR-7.3 ❌ 미구현(선택 항목). 공시 목록·제목 수준에서 판단하고 있다.
> - FR-7.5 ✅ 접수번호(`rcpNo`)를 클라이언트까지 전달해 원문 링크를 만든다.

### FR-8. 시산표(TB) 검증 — *초안에 없던 추가 기능*

감사인이 클라이언트 ERP에서 추출한 시산표를 업로드하면, **그 시산표 자체가
무결한지**를 먼저 확인한다. 이 검증을 통과해야 이후 분석과 표본추출의 기초로
신뢰할 수 있다.

- FR-8.1 표준 시산표 템플릿(`계정코드/계정과목/기초잔액/당기차변/당기대변/
  기말잔액`) 다운로드 및 업로드 파싱을 제공한다.
- FR-8.2 **차대변 균형**: 잔액은 차변 양수(+)·대변 음수(−)의 부호형으로 입력받아,
  전체 기말잔액 합계가 0인지 검증한다.
- FR-8.3 **당기 발생액 일치**: 당기차변 합계 = 당기대변 합계인지 검증한다.
- FR-8.4 **계정별 roll-forward**: 계정마다 `기초잔액 + 당기차변 − 당기대변 =
  기말잔액`이 성립하는지 확인하고, 불일치 계정과 차이금액을 목록으로 제시한다.
- FR-8.5 원 단위 반올림을 감안해 **±1원까지는 균형**으로 본다.

> **구현 메모 (FR-8)** — `src/lib/trialBalance.ts`. `시산표` 시트를 찾고 없으면
> 첫 시트를 시산표로 간주한다(단일 시트 export가 흔하므로). 검증은
> 계획적으로 오류를 심은 테스트 파일로 확인했다 — 차대변 불균형 −2,400,000원,
> 당기 발생액 차이 −2,500,000원, roll-forward 불일치는 재고자산 1건만 검출.

### FR-9. 전표(JE) 부정위험 테스트 — *초안에 없던 추가 기능*

ISA 240(재무제표감사에서 부정에 관한 감사인의 책임)이 요구하는 전표 테스트를
자동화한다. 각 테스트는 감사인이 추가로 들여다볼 **예외항목을 뽑아주는 것이지,
그 자체로 부정을 확정하지 않는다.**

- FR-9.1 다음 8종 테스트를 수행하고, 각 예외항목에 전표번호·일자·계정·금액·사유를
  붙여 제시한다.
  1. **주말 전기** — 토·일요일 입력분
  2. **심야 전기** — 22~05시 입력분
  3. **라운드넘버** — 100만원 이상이면서 100만원 단위로 딱 떨어지는 금액
  4. **적요 공란** — 거래 설명이 없는 전표
  5. **작성자 = 승인자** — 직무분리(SoD) 위반 / 통제 무력화
  6. **결산일 임박 전기** — 데이터상 최종일 기준 5일 이내(결산일 자동 추정)
  7. **드물게 쓰는 계정** — 전 기간 1회만 등장한 계정 (전표 30건 이상일 때만)
  8. **승인한도 회피** — 사용자가 승인한도를 입력하면, 한도의 95%~한도 미만 금액
- FR-9.2 **작성자 집중도**: 전표 작성자 상위 5명의 건수·비중을 표시한다.
- FR-9.3 전기일자/전기시각은 텍스트(`YYYY-MM-DD`, `HH:MM`)와 **엑셀 일련번호·
  시간 소수** 형식을 모두 파싱한다.
- FR-9.4 화면에 노출하는 예외항목은 테스트당 최대 50건으로 제한하되, 전체 건수는
  그대로 표시한다.

> **구현 메모 (FR-9)** — `src/lib/journalTests.ts`. 8종 모두 의도적으로 이상거래를
> 심은 테스트 파일로 검증했다(주말 10건, 심야 1건, 라운드넘버 2건, 적요공란 1건,
> 자가승인 1건, 결산일 임박 2건 — 결산일 2025-12-31 자동 추정, 희소계정 1건,
> 한도회피 1건). 이후 자동화 테스트(`src/lib/__tests__/journalTests.test.ts`)로
> 8종 전부와 엑셀 일련번호·시간 소수 파싱, 50건 표시 제한을 회귀 고정했다.

### FR-10. 중요성 산정 (ISA 320) — *초안에 없던 추가 기능*

중요성은 감사의 출발점이다. 어느 계정을 얼마나 파고들지, 표본을 몇 개 뽑을지,
발견한 왜곡을 넘길지 말지가 전부 여기서 나온다. 그래서 이 값은 조서(중요성
기준란)·MUS(허용왜곡금액)·미수정왜곡 집계표(FR-11)로 함께 흘러간다.

- FR-10.1 벤치마크를 4종(법인세차감전순이익/매출액/자산총계/자본총계) 중에서
  고르고, 금액은 재무제표에서 자동으로 채우되 사용자가 덮어쓸 수 있다.
- FR-10.2 적용률은 기준서가 숫자를 못박지 않고 감사인의 판단으로 두므로,
  실무 통용범위(예: 세전이익 5~10%, 매출액 0.5~1%)를 제시하고 그 안에서 고르게 한다.
- FR-10.3 3단으로 산출한다 — 전반중요성(OM) = 벤치마크 × 적용률,
  수행중요성(PM) = OM × 수행중요성률, 명백히 사소한 기준(CTT) = OM × 5%.
- FR-10.4 수행중요성률은 위험이 높다고 볼수록 낮게 잡아 여유를 크게 둔다
  (높음 50% / 보통 75%).

> **구현 메모 (FR-10)** — `src/lib/materiality.ts`. 법인세차감전순이익은 별도
> 계정으로 잡히지 않는 경우가 많아 **당기순이익 + 법인세비용으로 역산**한다.
> 손익 벤치마크는 적자일 수 있으므로 절대값을 씌우지 않고 그대로 돌려주고,
> 적자 여부는 화면에서 경고한다(적자 기업에 이익 기준은 부적합).

### FR-11. 미수정왜곡사항 집계표(SUM, ISA 450) — *초안에 없던 추가 기능*

감사 결론은 결국 여기서 나온다. 개별로는 사소해 보이는 왜곡도 합치면 중요성을
넘을 수 있어서, 발견한 왜곡을 한 곳에 모아 합계를 전반중요성과 비교한다.

- FR-11.1 왜곡을 사실/판단/추정 3유형으로 구분해 등록하고, 세전이익에 미치는
  영향(양수 = 이익 과대계상)을 입력한다.
- FR-11.2 미수정분의 **순합계(상계 후)** 와 **총합계(절대값)** 를 함께 제시한다.
  상계로 순액이 작아 보여도 왜곡의 총규모는 따로 봐야 하기 때문이다.
- FR-11.3 순합계가 전반중요성을 넘는지, 개별로 전반중요성을 넘는 항목이 있는지를
  각각 판정한다.
- FR-11.4 명백히 사소한 기준(CTT) 미만 항목 수를 따로 세어, 집계에서 빼도 되는
  항목을 감사인이 판단할 수 있게 한다.

> **구현 메모 (FR-11)** — `src/lib/misstatements.ts`.
> **전표(JE) 테스트의 예외항목은 여기로 자동 유입되지 않는다.** "주말에 전기됐다"는
> 조사 대상 신호이지 확인된 왜곡금액이 아니기 때문이다. 반면 시산표 roll-forward
> 불일치는 장부 자체의 확인된 차이라서 사실왜곡 후보로 제안하되, 감사인이 확정하기
> 전까지 `source: "trialBalance"`로 구분해 표시한다.

### FR-12. 기준서 AI 챗봇 (RAG) — *초안에 없던 추가 기능*

- FR-12.1 감사기준서 요지를 담은 코퍼스(`data/standards-corpus.json`, 임베딩 포함)를
  빌드 시점에 만들어 커밋하고, 런타임에는 질문만 임베딩해 코사인 유사도로 검색한다.
- FR-12.2 검색된 문서만 근거로 Solar가 답하게 하고, **최고 유사도가 임계값(0.28)
  미만이면 LLM을 아예 호출하지 않고 기권**한다(환각 원천 차단).
- FR-12.3 기권 시에도 가장 가까웠던 후보와 임계값을 함께 보여줘, 사용자가 질문을
  고쳐 쓸 단서를 남긴다(막다른 길 방지).
- FR-12.4 답변에 사용한 인용은 클릭하면 실제로 근거로 넘긴 요지 본문을 그대로
  보여준다 — "무엇을 근거로 답했는지" 검증 가능해야 하기 때문이다.

> **구현 메모 (FR-12)** — `src/lib/standardsRag.ts`. 임계값 0.28은 경험적으로
> 보정했다: 유효한 감사 질문은 0.32~0.51, 무관한 질문(날씨·코딩·요리)은 0.10~0.16로
> 갈려서 그 사이 값을 골랐다. 질문 문장은 Upstage로 전송되므로 FR-1.8 동의
> 게이트를 함께 통과해야 한다.

### FR-13. 선택적 서버 저장 + 감사증적 — *초안에 없던 추가 기능*

- FR-13.1 `SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`가 설정된 경우에만 분석 요청을
  서버에 저장하고, 없으면 `localStorage` 폴백으로 동작한다. **환경변수 없이도 전
  기능이 동작하는 것이 기본값이다.**
- FR-13.2 로그인 없이 소유를 구분하기 위해 브라우저 세션 id(uuid)를 최초 1회
  생성·보관한다.
- FR-13.3 삭제는 hard delete가 아니라 **soft delete + 이벤트 기록**으로 처리해
  증적을 남긴다(감사 도구가 스스로의 이력을 지우면 안 된다).
- FR-13.4 저장 대상은 **요약 재무제표까지**다. 거래 단위 원장(전표·시산표)은
  서버로 보내지 않는다.

> **구현 메모 (FR-13)** — `src/lib/supabaseServer.ts`(server-only 잠금),
> `auditStore.ts`(서버), `auditClient.ts`(클라이언트 래퍼), `supabase/schema.sql`.
> service_role 키는 절대 클라이언트 번들에 들어가면 안 되므로 `NEXT_PUBLIC_`
> 접두사를 붙이지 않는다.

## 8. 비기능 요구사항 (Non-Functional Requirements)

- **정확성/투명성**: 모든 이상탐지 결과는 "확정 결론이 아닌 스크리닝 지표"임을
  UI에 명시. 계산 근거(산식)를 함께 제공.
- **데이터 프라이버시/보안**: 비상장법인 업로드 데이터는 Supabase Storage의
  private 버킷에 저장하고 signed URL로만 접근한다. DART/Upstage API 키는
  서버 환경변수로만 관리하며 클라이언트 번들에 포함하지 않는다.
- **사용성**: 회계 도메인 전문가가 별도 학습 없이 사용할 수 있는 수준의 UI.
- **성능**: 단일 기업 재무제표 분석 기준 수 초 내 결과 반환. **Vercel Hobby
  플랜의 서버리스 함수 실행시간이 10초로 제한**되므로, 이를 초과할 수 있는
  대용량 연산(FR-3.6 등)은 데이터 규모를 제한하거나 클라이언트 처리로 분산한다.
- **가용성**: Supabase 무료 프로젝트는 **7일간 API 요청이 없으면 자동으로
  일시정지(pause)** 된다. 대회 제출/심사 전에는 반드시 프로젝트 접속 상태를
  확인하고, 필요 시 GitHub Actions 등으로 주기적 헬스체크(예: 매일 1회 API
  호출)를 구성해 비활성화를 예방한다.
- **배포/운영**: GitHub 저장소에 push하면 Vercel이 자동으로 빌드/배포하는
  CI/CD 흐름을 사용한다.

> **구현 메모 (8번)**
> - **가용성**: 기본 배포(환경변수 미설정)에서는 Supabase를 쓰지 않으므로 **7일
>   비활성 자동 일시정지 리스크가 없다.** 다만 FR-13 서버 저장을 켜기로 했다면
>   이 리스크가 되살아나므로, 그때는 심사·시연 전 프로젝트 활성 상태를 확인해야
>   한다(백엔드가 죽어도 앱은 localStorage 폴백으로 계속 동작하지만, 서버에 쌓인
>   요청 목록과 감사증적은 조회되지 않는다).
> - **데이터 프라이버시**: 초안(private 버킷 + signed URL)보다 강화됐다.
>   엑셀·시산표·전표 등 클라이언트 데이터는 **서버로 전송되지 않고 브라우저
>   메모리에서만** 처리되며, 거래 단위 원장 데이터는 `localStorage`에도 쓰지
>   않는다. 제3자 AI 전송은 명시적 동의 후에만 이뤄지고 회사명은 제외한다(FR-1.8).
> - **성능**: DART corpCode 검색이 빌드 시점 캐시로 바뀌어 런타임 다운로드가 없다.
> - **배포**: Vercel 프로젝트의 Root Directory가 `web`으로 설정돼 있다. 따라서
>   CLI로 배포할 때 `vercel --prod`는 **저장소 루트에서** 실행해야 한다
>   (`web/`에서 실행하면 경로가 `web/web`으로 중복돼 실패).

## 9. 기술 스택 (Technical Requirements)

확정된 환경: **Next.js / Supabase / Vercel / GitHub**, AI 기능은 **Upstage AI**.
아래 항목은 모두 이 환경에서 무료 티어로 동작 가능한 범위로 구체화한 것이다.

> **실제 채택 스택 (2026-07-27 기준, `web/package.json`)**
>
> | 초안 | 실제 | 사유 |
> |---|---|---|
> | Supabase (Postgres/Storage/Auth) | **선택(optional) 서버 저장만** | 초안이 맡기려던 역할(corpCode 캐시·체크리스트 매핑·파일/리포트 저장)은 전부 다른 방식으로 대체됐다. 지금 Supabase는 FR-13(서버 저장 + 불변 감사증적) 용도로만, 환경변수가 있을 때만 켜진다. Storage·Auth는 여전히 미사용 |
> | `@react-pdf/renderer` | **`jspdf` + `html2canvas`** | 한글 폰트 임베딩 회피, DOM 래스터화 |
> | `graphology` + React Flow/Cytoscape | **미사용** | FR-3.6이 그래프 사이클 탐지에서 2자간 매칭으로 재설계됨 |
> | — | **`adm-zip`, `fast-xml-parser`** | DART CORPCODE.xml 처리 |
> | — | **`vitest`** (devDependency) | 도메인 순수함수 회귀 테스트 (아래 참고) |
> | Next.js | **Next.js 16.2.10 / React 19.2.4 / Tailwind v4** | — |
> | `recharts` | **`recharts` 3.10** | 그대로 |
> | `xlsx`(SheetJS), `docx` | 그대로 | — |
>
> 소스 구성: `web/src/lib/`에 도메인 로직 21개 모듈(`ratios`, `anomalyDetection`,
> `journalTests`, `trialBalance`, `musSampling`, `materiality`, `misstatements`,
> `standardsRag`, `industry`, `reportExport`, `dart`, `upstage`, `auditStore` 등),
> `web/src/app/api/`에 Route Handler 11개, UI는 `app/page.tsx` 단일
> 파일(약 5,700줄) + `src/components/` 5개.
>
> **테스트**: `npm test`(vitest). `web/src/lib/__tests__/`에 도메인 순수함수
> 회귀 테스트 145건 — 재무비율·별칭 해석·Benford·Beneish·Altman·RSF·라운드트립·
> 전표(JE) 8종·시산표 검증·MUS·중요성·미수정왜곡 집계·ISA 인용 화이트리스트.
> UI(page.tsx)와 외부 연동(DART/Upstage/Supabase)은 범위 밖이다 — 키·네트워크가
> 필요하고 `server-only` 잠금 때문에 노드 테스트 환경에서 로드되지 않는다.

- **프론트엔드/풀스택 프레임워크**: Next.js(App Router, TypeScript)
- **배포/호스팅**: Vercel(Hobby 플랜, 무료)
  - 서버리스 함수 실행시간 최대 10초, 대역폭 100GB/월, 함수 호출 100K/월,
    동시 빌드 1개 — 개인/비상업적 용도 한정. 대회 출품(비상업) 범위에서는 충분.
- **형상관리/CI**: GitHub 저장소 + Vercel Git 연동 자동 배포
- **데이터베이스/인증/스토리지**: Supabase(무료 티어)
  - **Postgres**: DART corpCode 캐시 테이블(FR-1.2), 체크리스트 매핑
    테이블(FR-4.1) 저장. 무료 티어 한도: DB 500MB, 파일 스토리지 1GB, 월
    egress 5GB, 활성 프로젝트 최대 2개.
  - **Storage**: 업로드 엑셀 파일, 생성된 PDF/Word 리포트 저장(private 버킷 +
    signed URL).
  - **Auth**: MVP 범위에서는 사용하지 않음(로그인 없이 세션 단위로 동작).
    추후 확장 시 이메일/매직링크 로그인으로 개인별 분석 이력 저장에 활용 가능.
  - ⚠️ 무료 프로젝트는 7일 비활성 시 자동 일시정지 (8번 비기능요구사항 참고).
- **엑셀 파싱**: `xlsx` (SheetJS)
- **그래프 분석**: `graphology` 등 JS 그래프 라이브러리 (순환거래 사이클 탐지,
  FR-3.6) + 시각화: React Flow 또는 Cytoscape.js
- **차트/대시보드**: `recharts`
- **리포트 생성**: `@react-pdf/renderer`(PDF), `docx` npm 패키지(Word)
- **외부 연동(공공데이터)**: DART Open API (공시검색, 고유번호, 단일회사 전체
  재무제표) — 서버사이드에서만 호출
- **AI**: [Upstage AI](https://www.upstage.ai/)
  - Solar LLM(Chat Completions API): AI 공시요약(FR-7.2)
  - Document Parse(선택): 스캔/PDF 공시 문서 텍스트 추출(FR-7.3)
  - Information Extraction API(`POST https://api.upstage.ai/v1/information-extraction`,
    OpenAI Chat Completions 호환 형식 + `response_format.json_schema`로 추출할
    필드 스키마 직접 정의): 비상장법인 재무제표 PDF/이미지에서 핵심 지표를
    key-value로 추출(FR-1.6)
  - 요금제: 완전 무료가 아닌 **크레딧 기반 종량제**(Solar 계열 $0.15/1M
    입력 토큰, Document Parse 표준 $0.01/페이지, Information Extract 표준
    $0.04/페이지, VAT 별도). 신규 가입 시 **$10 무료 크레딧** 제공 확인됨
    (console.upstage.ai 가입 페이지 기준). 그 범위 내에서 호출량을 관리한다.

> 참고: 위 스택은 "코딩 비전공자가 7일 내 완성"이라는 제약 조건과 확정된
> Next.js/Supabase/Vercel/GitHub 환경을 전제로 선정된 것이며, Claude Code가
> 구현을 주도하고 사용자는 도메인 로직(감사 판단 기준)을 검토하는 방식으로
> 진행한다.

## 10. 일정 (7-Day Plan)

| Day | 내용 |
|---|---|
| 1 | Next.js(TypeScript) 프로젝트 초기화, GitHub 저장소 생성, Vercel 연동, Supabase 프로젝트 생성, 엑셀 템플릿 확정, DART/Upstage API 키 발급 |
| 2 | DART 연동 Route Handler + Supabase corpCode 캐시 테이블 적재 + `xlsx` 업로드 파이프라인 |
| 3 | 재무비율 자동계산 + 이상 변동 계정 탐지(FR-2) |
| 4 | Benford/Beneish/Altman/RSF/네트워크 이상탐지(FR-3) + 감사 체크리스트(FR-4, Supabase 매핑 테이블) |
| 5 | MUS 샘플링 계산기(FR-5) |
| 6 | 대시보드(recharts) + 리포트 export(FR-6) + AI 공시요약(FR-7, Upstage 연동) |
| 7 | 통합 테스트, Vercel 배포 최종 점검(Supabase 활성 상태 확인 포함), 데모 시나리오/발표자료 준비 |

## 11. 리스크 및 대응방안

| 리스크 | 영향 | 대응방안 |
|---|---|---|
| 코딩 비전공자의 첫 프로젝트 + Next.js/React 학습 곡선, 7일 제한 | 일정 지연 | 핵심 기능(FR-1~FR-7) 우선순위대로 완성, Claude Code가 구현 주도 |
| DART 데이터가 없는 비상장법인 분석 | 데이터 확보 어려움 | 표준 엑셀 템플릿으로 사용자 직접 입력 지원 |
| 이상탐지 모델의 오탐(false positive) | 신뢰도 저하 | "스크리닝 지표"임을 명시, 확정 결론이 아님을 UI/리포트에 고지 |
| Vercel Hobby 서버리스 함수 10초 실행 제한 | 대용량 데이터(전표 수만 건 등) 처리 시 타임아웃 | 데이터 규모 제한(FR-3.6), 필요 시 클라이언트 사이드 처리로 분산 |
| Supabase 무료 프로젝트 7일 비활성 자동 일시정지 | 심사 시점에 서비스 접속 불가 위험 | 제출 전 접속 확인, 필요 시 GitHub Actions로 주기적 헬스체크 구성 |
| Upstage 크레딧 소진 | AI 공시요약/재무제표 자동인식 기능 중단 | 호출 1회로 제한, 결과 캐싱, $10 무료 크레딧 소진 여부 사전 확인 |
| 실제 스캔 문서 품질 편차(흐릿한 스캔, 손글씨, 복잡한 표 레이아웃) | FR-1.6 자동인식 정확도 저하 | 인식 결과는 반드시 사용자 검토/수정 후 반영(자동 확정 금지), 표준 엑셀 템플릿(FR-1.3)을 항상 대체 경로로 유지 |
| 심사 기준(아이디어/실무성 중심) | 과도한 기술 투자로 시간 낭비 | 데모 스토리(왜 실무에 유용한지)에 시간 배분 강화 |

## 12. 성공 기준 (Success Metrics)

- 대회 심사: 실제 회계법인 실무 시나리오로 데모 가능한 완성도
- 포트폴리오: 채용 면접에서 "왜 이 기능을 넣었는지, 어떤 감사기준/이론에
  근거했는지" 설명 가능한 수준의 완성도
- 기술: 핵심 기능 7개(FR-1~FR-7)가 실제 데이터(DART 실제 기업 + 샘플 엑셀)로
  Vercel 배포 환경에서 정상 동작 확인

## 13. 미해결 사항 (Open Questions)

- [ ] 제품명(가칭) 확정
- [ ] 조서 리포트 양식 최종안 (회사 실제 양식 참고 여부)
- [x] ~~Supabase 프로젝트 리전 선택~~ → **Supabase를 사용하지 않기로 결정**(6번 참고)
- [x] ~~**ISA 인용 화이트리스트 가드**~~ → **2026-07-27 적용 완료**
      (FR-4.4 구현 메모 참고)
- [ ] **배포 정책 정리** — GitHub↔Vercel 연동으로 push 시 자동 배포되는데,
      `CLAUDE.md`에는 "명시적으로 요청했을 때만 배포"라는 규칙이 있어 서로 어긋난다.
      자동 배포를 끄거나 규칙을 갱신할 것. **(사용자 결정 대기 중)**
- [ ] **FR-2.4 산업평균 활성화** — 로직은 있으나 `data/listed-industry.json`이
      없어 비활성. DART 키로 `node scripts/build-listed-industry.js`를 1회 실행해
      인덱스를 만들어 커밋하면 켜진다.
- [ ] **`page.tsx` 분리** — 단일 파일 약 5,700줄로 전체 TS 코드의 절반을 차지한다.
      탭 패널 단위로 `components/`에 추출하는 안이 유력. **(사용자 결정 대기 중)**
- [ ] **잔여 lint 오류 2건** — `page.tsx`의 "setState synchronously within an
      effect"(4145행 음성인식 지원 감지, 4276행 검색 디바운스). 둘 다 SSR 불일치
      방지·입력 리셋을 위한 의도적 패턴이라 규칙을 만족시키려면 구조를 바꿔야 해
      보류했다. 나머지 lint 오류 15건은 v3.2에서 해소.
- [x] ~~중요성 산정 워크시트~~ → **FR-10으로 구현 완료**
- [x] ~~미수정왜곡사항 집계표(SUM)~~ → **FR-11로 구현 완료**
- [ ] 파트너 리뷰에서 제안됐으나 아직 미구현인 실무 기능: 특수관계자 거래
      매핑(ISA 550), 수익 cut-off 분석, 조회서 관리(ISA 505), 총계정원장(GL)
      전체 업로드. 이 중 **수익 cut-off 분석**이 기존 전표 데이터만으로 구현
      가능해 우선순위가 가장 높다(결산일 전후 매출 인식 전표를 기간귀속 관점에서
      검토 — FR-9의 결산일 임박 테스트와 데이터·UI를 공유할 수 있다).

## 14. 범위 제외 (Out of Scope)

| 제외 항목 | 제외 사유 유형 | 상세 설명 |
|---|---|---|
| AI 계약서 검토 (리스/매출/차입 계약서 LLM 분석) | 시간/난이도상 무리 | Upstage Document Parse로 텍스트 추출 자체는 가능하나, 리스/수익인식 등 K-IFRS 조항 판단 기준과 체크리스트 설계까지 필요해 7일 + 비전공자 조건에서는 리스크가 큼 |
| 실시간/지속적 감사(Continuous Auditing) 시스템 | 시간/난이도상 무리 | Supabase Realtime으로 실시간 데이터 스트림 수신 자체는 기술적으로 가능하나, 지속적 감사에 필요한 트리거 규칙·알림 체계 설계는 7일 범위를 초과 |
| 대용량 전표 데이터(수만 건 이상) 처리 | 기술적 제약 | Vercel Hobby 플랜 서버리스 함수의 10초 실행 제한으로 대규모 배치 연산이 시간 초과될 수 있음. FR-3(이상탐지 모델 중 전표 기반 기법: FR-3.1, 3.4~3.6)의 처리 범위를 전표 1만 건 이하로 제한 |
| 산업평균 비교용 유료/공식 통계 데이터 연동 (예: 통계청, 신용평가사 산업통계) | 기술적 제약 + 시간상 무리 | 무료로 이용 가능한 업종별 재무비율 통계 API가 마땅치 않고 자체 수집도 시간이 부족함. 대신 동일 업종코드(KSIC)의 DART 등록 상장기업 재무제표를 직접 비교군으로 활용(FR-2.4) |
| Supabase Auth 기반 회원가입/로그인 및 개인별 분석 이력 저장 | 시간상 무리(MVP 범위 초과) | 기술적으로는 Supabase Auth로 간단히 구현 가능하나, 원래 요구사항(FR-1~FR-7)에 로그인/개인화가 포함되어 있지 않아 MVP 범위에 추가하지 않음. 추후 확장 여지로만 남겨둠 |
| Vercel Pro 플랜 수준의 상업적 운영(과금, SLA 등) | 기술적 제약 | Vercel Hobby 플랜은 비상업적 개인 프로젝트 용도로 제한됨. 대회 출품/포트폴리오 목적에는 해당 없음 |

## 15. 변경 이력 (Changelog)

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-07-20 | 최초 기획 |
| v2.0 | 2026-07-20 | 기술스택 확정(Next.js/Supabase/Vercel/GitHub/Upstage) 반영 |
| v3.0 | 2026-07-27 | 구현 현황 반영. 포지셔닝 재정렬(§1.1), Supabase 전면 미사용, FR-3.6 라운드트립 재설계, FR-8·FR-9 신규 추가, 리포트 명칭/라이브러리 변경, 미해결 사항 갱신 |
| v3.1 | 2026-07-27 | FR-4.4 ISA 인용 화이트리스트 가드 적용 (LLM의 가짜 기준서 인용 차단) |
| v3.2 | 2026-09-02 | 문서-코드 불일치 정리. FR-3.3을 Z'-Score(장부가 기준)로 정정하고 `/api/stock-price`의 용도를 밸류에이션 지표로 바로잡음. 코드에는 있으나 PRD에 없던 FR-10(중요성)·FR-11(미수정왜곡 집계)·FR-12(기준서 RAG 챗봇)·FR-13(선택적 서버 저장) 등재. Supabase 서술을 "전면 미사용"에서 "선택 백엔드"로 정정. FR-2.4를 "미구현"에서 "코드 존재·데이터 미생성"으로 정정하고 활성화 절차 명시. 회귀 테스트 스위트(vitest 145건) 도입 반영 |

### 주요 커밋

| 커밋 | 내용 |
|---|---|
| `23c203a` | 최초 커밋 — Next.js 앱 + 폐기된 Python 프로토타입 아카이브 |
| `67cee58` | 분석 패널 탭 구조화 + ISA 기준서 조회 모달 (FR-4.3) |
| `3df705c` | MUS 샘플링 계산기 + AI 공시요약 핵심기능 소개 (FR-5, FR-7) |
| `a19583b`, `51d6604` | DART corpCode를 빌드 시점에 사전 생성해 프로덕션 검색 멈춤 해결 (FR-1.2) |
| `6fc61db` | 파트너 리뷰 지적 3건 반영 — 라운드트립 탐지 재설계, 기밀성 강화(원장 미저장·AI 동의), 과장 카피 수정 (FR-3.6, FR-1.8, §1.1) |
| `8168ce2` | export를 실제 조서 초안 수준으로 보강(조서번호·서명란·결론·틱마크), DART 조회 시 분석 자동 표시 (FR-6.2) |
| `d855844` | 탭을 '재무제표 스크리닝' / '감사 실무' 두 그룹으로 분리 (§1.1) |
| `78b6464` | 모델 한계 고지 추가, MUS 확장계수 정정(99% 2.0→1.9), 신규 계정 표시 |
| `28797c3` | Benford 강화 — 표본크기에 휘둘리지 않는 MAD 판정 + 첫 두 자리 검정 (FR-3.1) |
| `512681a` | 선택적 Supabase 백엔드 + 불변 감사증적 (FR-13) |
| `277b91d` | 근거기반(RAG) 기준서 AI 챗봇 — 근거 약하면 기권 (FR-12) |
| `ec77e1f` | ISA 320 중요성 산정 워크시트 + 저장모드 배지 (FR-10) |
| `0727608` | ISA 450 미수정왜곡사항 집계표 (FR-11) |
| `cae93b7` | 결산일을 추정하지 않고 감사인이 직접 입력하도록 변경 (FR-9.1) |
| `880b102` | 샘플 전표·시산표·재무제표가 서로 대사(tie-out)되도록 정합화 |
